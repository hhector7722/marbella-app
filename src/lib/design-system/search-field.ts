/**
 * Contrato SearchField — buscador compacto (no es Field).
 * Anatomía: lupa + input. Sin etiqueta uppercase.
 * Altura bloqueada a tactil.minimo; el ancho lo decide el sitio.
 */

export const SEARCH_FIELD_COMPONENT_ID = 'SearchField' as const;

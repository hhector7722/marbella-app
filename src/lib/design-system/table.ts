/**
 * Contrato Table / T8 — tabla operativa.
 * Composición: <table data-component="Table"> + thead de marca + tabular-nums.
 * No es un componente Table con props de columnas.
 */

export const TABLE_COMPONENT_ID = 'Table' as const;

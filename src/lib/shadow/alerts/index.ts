/**
 * Alertas de migración (fuera del alcance P0–P8 actual).
 * @internal scaffolding
 */
export function emitShadowAlertNotImplemented(): never {
  throw new Error('shadow/alerts: no implementado (post Fase 1 core)');
}
